services:
  fleet-command:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: strait-of-hormuz-command
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: unless-stopped